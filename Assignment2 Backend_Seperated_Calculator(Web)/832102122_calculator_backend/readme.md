The backend includes the respond function file(views.py), the models to be used in the database(models.py),
the url setting file(urls.py) and the database file.
They can all be found in the src folder.